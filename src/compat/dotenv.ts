// Browser environment adapter: the console has no host environment file to load.
// Follows the ATS v8 browser configuration convention; see remediation evidence.
export function config() {
  return {};
}

export default { config };
