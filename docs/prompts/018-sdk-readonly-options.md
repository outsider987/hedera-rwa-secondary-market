# SDK read-only research authorization — September 6, 2026

Victor replied **“好確認下”** after the previous result proposed confirming the official read-only initialization path. This activates the research ticket proposed in HANDOFF at `0bf268105a9c1a2474dcd3bfb844652997be7e2d`. The former handoff base `938aeb71838c6e65e18bb82d499d12c78698cbda` was verified as an ancestor.

Confirm supported public initialization and transport controls in the pinned SDK using installed source, official documentation and upstream guidance. Report gaps honestly and identify the smallest concrete future change and exact files. Do not repeat the known missing-export browser probe without a changed candidate. No dependency patch, fabricated validator, deep import, wallet initialization, VC, mutation or external message is authorized.

Allowed files: `docs/evidence/016-sdk-readonly-options.md`; a same-stem MJS/JSON only if a concrete new bounded probe is justified; this prompt; `docs/ai-usage/022-sdk-readonly-options.md`; HANDOFF, plan, ATTRIBUTION and AI_USAGE. Preserve historical evidence and all T01/manual acceptance requirements. Commit research and handoff together; no automatic push, merge or next-ticket implementation.

Decision: source review found no qualifying public path. Preserve the prior runtime evidence, add no redundant probe, and propose an isolated export/provider compatibility trial for separate activation. `Network.init` is a documented broader API, not a missing API; its wallet discovery and listeners make it unsuitable here. Genuine request validation and existing chain/address verification must remain intact.
