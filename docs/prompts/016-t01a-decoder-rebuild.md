# T01a original-schema regeneration — September 6, 2026

Victor replied **“Go”** after the diagnosis identified the npm workaround and proposed rebuilding the original schemas to clear the remaining 12 security checks. This activates the handoff's bounded regeneration ticket from `301833451e18cb993b4db480116f82def4b870e6`; no further permission is needed within this scope.

Keep ATS 8.0.0, both SDK/proto parents, Node/npm/framework pins and original schemas unchanged. Use exact protobufjs-cli 1.3.3/runtime 7.6.6 with the previous four qualified parent overrides; apply the proven targeted nested update. Use the installed compiler's static-module generation with upstream semantic flags and its built-in ES6/CommonJS wrappers, checking the public API rather than adding a Babel dependency. No handwritten decoder patches or schema edits.

Exact files: `package.json`, `package-lock.json`, `scripts/rebuild-proto.mjs`; new evidence `014-t01a-decoder-rebuild.test.mjs`, `.md`, `.json`, `.patch` only on rejection; this prompt; work record `020-t01a-decoder-rebuild.md`; HANDOFF, plan, ATTRIBUTION, AI_USAGE. Reuse previous tests/harnesses without overwriting historical evidence. Scratch generation/build/browser work is isolated first.

Require schema hash/inventory and parent/compiler guards, deterministic repeat output, clean install/rebuild, all existing public API/wire/64-bit/security checks, npm test/typecheck/build, complete audit/lock accounting and official-entry dev/preview browser checks. Retain upstream license notices; do not vend generated source into Git. Make a successful repair reproducible through project scripts, without approving third-party install scripts. Stop and restore candidate dependency/code changes on incompatibility, failed security gate or an uncovered new advisory. A test-harness defect may be corrected without weakening its asserted behavior.

No wallet/provider, signer, chain request, SDK config integration, VC or NOVA operation. B2 and remaining T01 requirements remain open. Commit records with the result; do not push or merge automatically.

Implementation findings: the old archive needs unambiguous resolution of relocated imports. All original files remain hash-checked; parsing produces an intermediate reflection descriptor for pbjs. Match the upstream shell-expanded entrypoints (125 old / 174 flat new), loading all their imports; recursively selecting every archived file incorrectly adds 53 public types. Keep the exact public API oracle unchanged. Because runtime deduplication merges the old shared `hashgraph` registry, use pbjs's private per-package/version root names; a new load-order regression test must pass. These are generator implementation choices preserving original public behavior, not schema/SDK/asset changes.
