# T01b-4: official SDK config — September 6, 2026

Victor replied **“Go”** after the previous result named official ATS SDK config integration as the next ticket. This activates the exact T01b-4 scope in HANDOFF from `938aeb71838c6e65e18bb82d499d12c78698cbda`; the previous handoff base is an ancestor.

Use the pinned ATS 8.0.0 public entry, fixed Testnet RPC/Mirror, Resolver and Equity config. Keep existing deployment verification and require a safe integer SDK payload >= 1. Inspect initialization first; no wallet prompt, signing, transaction, VC, SDK upgrade, new dependency or expanded install-script approval. Use TDD and preserve manual acceptance requirements. The public query must support the existing cancellation/deadline, no automatic retry and request-boundary requirements. If it cannot, retain reproducible diagnostics and stop rather than bypassing the SDK.

Allowed source/tests: `src/ats.ts`, `src/App.tsx`, `src/deployment.ts`, `tests/ats.test.mjs`, `tests/shell.test.mjs`, `tests/deployment.test.mjs`. Evidence: new `015-t01b-4-sdk-config.mjs`, `.md`, `.json`. Documents: this prompt, work record `021-t01b-4-sdk-config.md`, HANDOFF, plan, ATTRIBUTION and AI_USAGE. Historical evidence is not overwritten. Scratch browser candidates may be generated by the evidence harness; application changes are retained only if the gate passes.

Validate npm ci/test/typecheck/build, public payload validation, rejection/timeout/cancellation and late results, real official-entry dev/preview browsers, separate live and synthetic reads, and full rendered dependency membership. Preserve B2 before VC or related installation changes. Commit code/diagnostics and documents together; no automatic push, merge or next-ticket implementation.

Observed boundary: root import succeeds, but `SetNetworkRequest` is not publicly exported and the initializer rejects plain objects without `validate()`. The retained harness narrows to this prerequisite; later SDK query/transport cases remain unexecuted. Cold dev import was separated from the public-read deadline, and the negative-error check accepts minified variable names while still requiring a TypeError for the missing validator. No assertion was changed to accept a missing public constructor as successful integration.
