# T02 report and authorized push — September 7, 2026

Base: `ea66aa35299a33a877c31fbb112a65dd1460a827`. Victor supplied
`holdbook-public-evidence (3).json` and requested a report mapping this work to
screenshots/files, followed by push. This explicitly authorizes pushing the
current branch and supersedes the earlier no-automatic-push boundary for this
operation; no merge or next-ticket implementation is authorized.

Codex checked the exact public file against evidence 024. It is an older
sender-format mismatch at block 40209380, not a repaired successful export.
The transaction/calldata/VC digest match the independent complete result.
Codex produced a concise Markdown index, standalone offline HTML with ten
unchanged original PNGs and four public JSON downloads, all 56 comparisons,
source links, and a structured manifest preserving export (3). No full VC,
signature, browser profile, secrets or arbitrary wallet data was collected.

Affected files: `docs/evidence/025-t02-followup.{md,json,html}`, this entry,
AI_USAGE, HANDOFF and plan. Existing source and historical evidence are unchanged.
Validation covers public field projection, digest identity, screenshot hashes,
local links and embedded JSON. Offline browser checks passed at 1440/390 px:
ten decoded images, 56 rows, four correct downloads, keyboard link focus and
no overflow, errors or external requests. Prior
application install/tests/build/live checks remain in evidence 024; none are
claimed newly rerun. No new chain reads or mutations are needed for this report.

Native BBS exclusion, audit/peer/license gaps, human-attestation limits and the
missing final human screenshot/export remain explicit. T02 remains complete on
independent verified evidence; T03 stays inactive. Push the current branch
normally to its configured origin and verify remote HEAD afterward.
