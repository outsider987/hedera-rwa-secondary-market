# T01b-2: deployment reads — 2026-09-05

- **Human decisions:** Victor approved the [proposed plan](../prompts/011-t01b-2-deployment.md), requested TDD and scheduled real MetaMask acceptance for September 6 without dropping requirements. This branch is not approved for push/merge.
- **AI assistance:** Codex implemented the independent viem/Query deployment check, tests and documentation using the Ponytail workflow. No delegated or human code review is claimed.
- **Affected files:** `src/deployment.ts`, `src/App.tsx`, `src/styles.css`, `tests/deployment.test.mjs`, `tests/shell.test.mjs`; this ticket's `docs/evidence/009-t01b-2-*`, prompt, plan authorization, handoff, attribution and AI index. Dependencies, wallet implementation and ATS diagnostics remain unchanged.
- **Checks:** 20 core Node tests failed before implementation and passed afterward; the final expanded suite passes 31 tests. Browser red/green also caught old verification resurfacing after an unexpected query error. Final deployment/wallet browser cases, real Testnet reads in dev/preview, npm ci/typecheck/build and bundle membership checks are linked in the [one-page evidence](../evidence/009-t01b-2-deployment.md).
- **Implementation choices:** use viem's individual getChainId/getCode actions on a client, reducing the initial 541 kB public-client bundle to 311 kB. Bundle verification compares final build output with dist; an early generateBundle comparison ran before asset finalization and was corrected. No new dependencies or speculative framework.
- **Limits:** real MetaMask and account Mirror acceptance remain Pending in the complete handoff checklist. T01a security, peer mismatches, ATS config and VC requirements remain open. Public deployment reads are not signatures/transactions or proof of ATS compatibility. Historical evidence is preserved; the unseen pre-event research draft remains uninspected.
- **Git:** clean start `66dea387a64ef385e5e84d3f7eb0f62699ed69cf`, branch `feat/t01b-2-deployment-check`; prior handoff base verified as an ancestor. Code and records are committed together; actual final commit is read from Git.
