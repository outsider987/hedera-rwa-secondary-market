# T01b-4 app SDK config integration — September 6, 2026

- Victor's “接吧” activated the exact main-app integration in [Prompt 020](../prompts/020-t01b-4-sdk-integration.md). Base: `95b00f958e5487a1680e3e277c49397d4ff79947`; the prior handoff base is an ancestor. No push/merge, VC or NOVA work was activated.
- Codex used Ponytail guidance, existing app/SDK-trial code, installed ATS/ethers source, Node tests and external Playwright. No subagents, external reviewers or maintainer responses.
- Added manual SDK preparation/config controls, shared read deadline/transport constants, safe payload validation, cancellation/provider cleanup and invalidation on wallet transitions. Retained the existing deployment query, wallet behavior, ATS diagnostic and dependency pins.
- TDD: new orchestration and shell checks failed before implementation, then passed. The first lifecycle browser run exposed a fixture error: programmatic focus after a pointer click does not activate `:focus-visible`; the fixture now enters keyboard modality before checking keyboard focus. No CSS workaround was added. A syntax error in the new static-bundle assertion was corrected before the final matrix started; no app workaround was required.
- Actual verification and limitations: [evidence 018](../evidence/018-t01b-4-sdk-integration.md). Clean install, 89 Node tests, typecheck/build, 116 SDK browser cases (four live payloads of `1`) and 20 existing wallet cases pass. Production assets match the harness; all 359 rendered package locations are accounted for. Audit retains 80 findings and two peer problems. No dependency/patch changes were made. No real MetaMask acceptance, signature or transaction is claimed.
- Files: `src/ats.ts`, `src/App.tsx`, `src/deployment.ts`; ATS/shell tests; evidence 018 MJS/MD/JSON; prompt 020; this record; HANDOFF, plan, ATTRIBUTION and AI_USAGE. Historical evidence and the manifest/lock/patch remain unchanged.
