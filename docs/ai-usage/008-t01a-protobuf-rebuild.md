# T01a bounded decoder rebuild trial — 2026-09-05

- Human decision: [「好」 accepting the concrete prior proposal](../prompts/009-t01a-protobuf-rebuild.md).
  Clean base `b5640298d0839640d444135da1180f0d5461bd11`; prior documented
  `fbe9b9f` verified as ancestor. T01a only, T01b inactive.
- Codex read repository rules, handoff and applicable plan/authorization, then
  public pinned package metadata, original schemas, build flags and npm matcher/
  edge handling. Ponytail guidance favored the existing compiler and test runner.
  No subagent, outside reviewer or mentor was consulted.
- Installed/audited an isolated exact compiler/runtime graph with scripts ignored;
  tested the accepted four version-qualified overrides plus those exact development
  dependencies in the project graph. npm ci succeeded. The targeted protobufjs
  lock update left bytes unchanged; npm ls failed on two retained Hashgraph
  7.2.5 copies. Pure rule matching from the previous research did not establish
  full graph acceptance. No definitive npm internal root cause is claimed.
- The conflict stop condition ended the trial before decoder generation. A draft
  Node generator was written but never executed and was deleted. Package files
  were restored byte-for-byte to base, followed by npm ci. No generated upstream
  file changed. The rejected package/lock diff remains evidence only.
- Kept one Node built-in diagnostic extending the preserved 005 cases: synthetic
  ThresholdKey recursion, declared-length overruns and public namespace snapshots.
  [Evidence](../evidence/007-t01a-protobuf-rebuild.md) records full source/lock/audit
  inventories, finite child probes and actual checks. Before/restored diagnostic:
  17 passed, 17 failed as required; six existing npm regression tests, restored
  npm ls, typecheck and build passed. Browser results are indexed in the evidence.
- Affected files: the new `docs/evidence/007-t01a-protobuf-rebuild*` test/records,
  this entry, AI_USAGE index, current HANDOFF, plan authorization/outcome,
  ATTRIBUTION and this round's prompt. Application/UI, browser harness, original
  tests/evidence, dependency files and pinned asset parameters remain unchanged.
- Final record checks: 22 allowed changed/new files, 69 local links/anchors,
  valid new JSON, original manifest/lock and all schema/generated hashes restored;
  no compiler remains in the project dependency tree. Local dev/preview servers
  were stopped after the four passing browser cases. Git whitespace checks passed.
- No install-script approval, wallet/provider access, chain/VC operation,
  signer, secrets/.env/profile access, upstream source vendoring, mentor message,
  deployment, push, merge or human review. No model identity, upstream acceptance,
  project-license or event-eligibility decision was invented. B1/B2 remain;
  no MetaMask action is required. The unseen pre-event draft remains uninspected.
