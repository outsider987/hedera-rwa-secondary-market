# Complete recorded demo experience — September 10, 2026

`based_on_commit: 765c53c3f7c32eedbb84cb159bc256092b6c85db` verified in Git;
clean starting tree on feat/t08-settlement. The user supplied the complete
3D implementation plan and requested all phases. [Prompt034](../prompts/034-demo-experience.md)
and [plan011](../plans/011-demo-experience.md) preserve intent and scope. Codex
used ponytail, impeccable and animate; the skill-required independent AI finish
review examined source and captures. Victor has not yet performed this ticket's
narration trial or human visual acceptance. No model identity is inferred.

Delivery: Overview composition, four finite3D narratives and HTML Proof;
manual/180-second Demo Mode with URL, keyboard, pause and route retention;
read-only Market top-five depth/new-server-match feedback/statuses. The existing
certificate, detailed NOVA story, four T08 cases and trading controller remain.
A public presentation snapshot preserves source locations and distinct dates;
T07 matches are never presented as the T05 swap. Every proof balance is historical.

Affected files: App, OverviewPage, MarketPanel, package/lock; new presentation
components/scenes/state/adapter/styles/runtime notices, presentation.json and dedicated Node
checks. Documents: plan011, prompt034, evidence050 harness/JSON/captures/logs,
this entry, HANDOFF, AI_USAGE, DESIGN, PRODUCT, ATTRIBUTION, DEMO and SUBMISSION.
No backend, contract, wallet core, SDK repair, API schema or matching algorithm
changed. No signature, chain mutation, push, merge, public deployment or message
to a third party occurred. Current dependency sources/licenses are in ATTRIBUTION.

Validation: npm ci completed with pinned Node24.19.0/npm11.17.0;121 application
and36 protobuf tests passed; typecheck and both app/showcase builds passed.
No existing resolved package version changed;13 new package locations. Existing
62 audit findings and upstream install-script/eval/chunk warnings remain.
Detector returned32 palette/type advisories and one palette warning on the
existing purple Admin heading; the user explicitly pinned these colors.

Corrections during verification: guarded browser globals for the existing
server-render test; restarted dev after npm ci invalidated optimizer imports;
fixed compliance's misleading Buyer endpoint and Proof fallback's incorrect
asset position following independent review. Intermediate captures affected by
HMR were replaced. The browser harness was corrected to allow the visible
Market Canvas, blur focus without clicking beneath the sticky header, and wait
for lazy scene loading/motion before measuring idle draw calls. Completed-match
selection requires the existing Completed filter; blocked read-only JSON-RPC
balance requests are classified separately from writes. Fault injection found
that R3F9.7 renderer initialization rejects outside its React boundary; a native
WebGL2 capability probe now selects the static fallback before mounting Canvas.
The temporary probe context is explicitly released. Minified chunks omit
upstream license comments, so a footer-linked runtime-notice asset retains the
original package texts and R3F’s official v9.7.0 license. Cold Demo entry also reads
the existing public settlement record to keep unresolved recovery notices visible.
The completed timeline originally exposed duplicate Play buttons; the terminal
state now has one. Demand-loop frame delta includes idle time, so manual motion
now measures from its first active frame; a draw-call regression checks movement
after a deliberate idle pause. Rendering DPR is 0.75 following software-rendered
long-tail observations; this does not establish recording-device performance.

The final full run reached181.222 seconds; its controlled context-loss injection
raced remount initialization. The controls-only rerun waits for readiness and
passes; native WebGL loss passes at all three sizes. Final DPR0.75 rAF medians
are16.7ms; p95 is50/33.4/33.4ms for1920/1440/390. Desktop sustained60fps is not
established on SwiftShader. Final full Node tests pass after the motion fix.

Final browser results and remaining limits are recorded in
[evidence050](../evidence/050-demo-experience.md), with its raw JSON and captures.
Browser checks use isolated public fixtures and an unsigned wallet double;
they do not repeat MetaMask or on-chain acceptance. Performance observations
identify the actual browser/rendering backend and emulated viewports, not an
invented physical mobile device. Victor's recording-device trial remains human
work. This ticket stops at a local commit; next ticket/allowed files: none.
